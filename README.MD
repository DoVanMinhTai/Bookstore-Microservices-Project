# Book Store

![Swagger UI Screenshot](screenshot/image1.png)


---

## Tech Stack
- Java 
- Spring Boot 3.4.2
- Next.js
- Keycloak
- Nginx

## Note

This project requires a `.env` file with the following configuration:

### .env

```env
```
## Installation
```
```

### You might also want to explore:
1. `http://pgadmin.local/`. Account login: `admin@adc.com` / admin. Register a server: postgres, port 5432, username admin, password admin. The Postgresql server is also exposed to the host machine: servername: localhost, port: 5432, username: admin, password: admin
2. `http://api.local/swagger-ui/` for all the REST API document of all the services
3. `http://identity/` for Keycloak console, account admin/admin

