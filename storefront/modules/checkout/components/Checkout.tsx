import React from 'react'
import { Checkout } from '../model/Checkout'

interface Props {
    checkout: Checkout
}

export default function CheckoutComponents({checkout} : Props) {
    return (
        <>
                        
        </>
    )
}
